# Leave empty (or omit this file) if frontend and backend are on the same domain.
# Set this when your frontend is deployed separately from the backend.
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
